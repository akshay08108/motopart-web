# Replace the built-in demo repository with real provider credentials here later.
NEXT_PUBLIC_API_MODE=demo
# Reuses the same Google Places integration pattern as DiagHub.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
